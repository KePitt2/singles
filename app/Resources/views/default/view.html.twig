{% extends 'base.html.twig' %}

{% block body %}
    <img src="{{ asset('/uploads/profiles/' ~ single.id ~ '.jpg') }}" alt="" />
    <p>{{ single.description }}</p>
{% endblock %}

{% block stylesheets %}
    <style>
        {{ parent() }}
        
        img{
            width: 100%;
        }
        p{
            font-size: 50px;
            padding: 20px 50px;
        }
    </style>
{% endblock %}